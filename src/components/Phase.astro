---
import SvgIcon from "./SvgIcon.astro";

const { icon, counter, heading, subheading } = Astro.props;
---

<div class="row">
  <h2 class="counter">{counter}</h2>
  <div class="details">
    <h2>
      {heading}
    </h2>
    <p>{subheading}</p>
  </div>
</div>

<style>
  .row {
    display: flex;
    align-items: flex-start;
    gap: 4rem;

    margin: 4rem 0;
  }

  .counter {
    font-size: 3.5rem;
    text-align: right;

    width: 3rem;
    margin: 0;
  }

  .details {
    flex: 1;
    text-align: left;

    margin: -1rem 0;
  }

  .details h2,
  .details p {
    margin: 1rem 0;
  }
</style>
