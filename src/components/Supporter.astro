---
const { src, title, maxHeight } = Astro.props;
---

<img src={src} alt={title} title={title} />

<style define:vars={{ maxHeight }}>
  img {
    height: 2.5em;
    max-height: var(--maxHeight);
    max-width: 10em;

    vertical-align: middle;
    margin: 1em;
  }
</style>
